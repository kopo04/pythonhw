#include <stdio.h>
// Hello World
/*
Hello World
*/

void main()
{
    double a, b, c;
    scanf("%lf %lf %lf", &a, &b, &c);
    if (c == 0)
    {
      printf("0이 아닌 정수를 입력 하세요.");
    } else {
      double z = (a + b) * c - (a - b)/c;
      printf("%lf", z);
    }
    
}
